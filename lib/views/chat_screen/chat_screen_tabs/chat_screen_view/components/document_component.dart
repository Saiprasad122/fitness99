import 'package:flutter/material.dart';

class DocumentComponent extends StatelessWidget {
  const DocumentComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
