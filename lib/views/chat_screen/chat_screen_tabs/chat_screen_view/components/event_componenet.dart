import 'package:flutter/material.dart';

class EventComponent extends StatelessWidget {
  const EventComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
