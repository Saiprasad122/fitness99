class ActivityClass {
  String title, description, location, fromTime, toTime, notes;

  ActivityClass({
    required this.title,
    required this.description,
    required this.location,
    required this.fromTime,
    required this.toTime,
    required this.notes,
  });
}
