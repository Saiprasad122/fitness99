import 'package:flutter/material.dart';

class CustomListActivityShimmer extends StatelessWidget {
  const CustomListActivityShimmer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
